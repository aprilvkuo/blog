# Cloudflare Pages 配置
name = "egguo-blog"
compatibility_date = "2024-01-01"

# KV Namespace 绑定
# 注意：需要先用 `wrangler kv:namespace create "stock-analysis-tasks"` 创建实际 namespace
# 然后将返回的 id 填入下方
kv_namespaces = [
  { binding = "TASKS_KV", id = "" }  # 替换为实际的 KV namespace ID
]
